<%= form_for [:admin, @artist], :html => {:multipart => true } do |f| %>
  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% @artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, 'Nom' %><br>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :description, 'Description' %><br>
    <%= f.text_area :description, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :image, 'Image (fichier)' %><br>
    <%= f.file_field :image %>
  </div>
  <div class="form-group">
    <%= f.label :remote_image_url, "ou l'url de l'image" %><br />
    <%= f.text_field :remote_image_url, class: 'form-control' %>
  </div>
    <div class="actions">
      <%= f.submit 'Ajouter',  class: 'btn btn-success' %>
    </div>
<% end %>
